#include <iostream>
using namespace std;

int main()
{
    int n;
    cin >> n;
    int s = 0;

    for (int i = 1; i < n + 1; i++)
    {
        s += i;
    }
    for (int i = 0; i < n - 1; i++)
    {
        int a;
        cin >> a;
        s -= a;
    }

    cout << s << "\n";
    return 0;
}
